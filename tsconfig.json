{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // v2：src/ demo 区已迁移——JSX 类型用 actview（vue 组件 + React 语义）
    "jsxImportSource": "actview"
  },
  "include": [
    "src",
    "packages/core",
    "plugins/*/src",
    "plugins/*/test"
  ]
}
