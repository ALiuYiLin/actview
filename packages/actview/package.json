{
  "name": "actview",
  "version": "1.0.5",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@actview/core": "workspace:*",
    "@actview/jsx": "workspace:*"
  },
  "scripts": {
    "build": "tsup && node ../../scripts/rewrite-package.mjs .",
    "publish": "npm version patch && pnpm build && cd dist && npm publish"
  }
}
